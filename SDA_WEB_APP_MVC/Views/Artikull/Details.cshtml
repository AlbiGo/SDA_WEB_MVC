@model Artikull

<div>
    <h1>
        Details for @Model.Name
    </h1>
    <p>
        @Model.Description
    </p>
    <p>
        @Model.DateCreated
    </p>
    <img src="@Url.Content(string.Format(Model.PhotoUrl))"/>
</div>